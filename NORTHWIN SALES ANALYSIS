/*Which sale employees from NorthWin should receive bonuses*/
SELECT e.EmployeeID, e.LastName, e.FirstName,o.OrderID, o.CustomerID, o.OrderDate,o.ShipperID,od.ProductID, od.Quantity, p.Price, p.SupplierID, p.CategoryID
FROM EMPLOYEES e
INNER JOIN ORDERS o
ON e.EmployeeID = o.EmployeeID
INNER JOIN ORDERDETAILS od
ON o.OrderID = od.OrderID
INNER JOIN Products p
ON od.OrderID = p.Price
;

SELECT *
FROM PRODUCTS
;
